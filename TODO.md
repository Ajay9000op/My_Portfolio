# TODO

- [ ] Fix `index.html` broken/invalid markup (bad newline in image src, invalid `<Kalakada-...>` tag, empty img src, typo `descriptison`, fix AOS attribute typo).
- [ ] Fix `assets/js/main.js` IIFE wrapper (`!(function($){...})(jQuery);` -> `(function($){...})(jQuery);`).
- [ ] Quick sanity check by loading `index.html` and one `projects/*.html` page.

